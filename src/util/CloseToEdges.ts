import { VerbalObject, Point } from "./../type/VerbalObject";
export class CloseToEdgesMap {
  private mapLeft: Map<number, number> = new Map();
  private mapTop: Map<number, number> = new Map();

  public isHasNumber(num: number) {}

  public addNumber(obj: VerbalObject) {
    obj.boundingBoxp1;
  }
}
