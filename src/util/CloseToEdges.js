export class CloseToEdgesMap {
    constructor() {
        this.mapLeft = new Map();
        this.mapTop = new Map();
    }
    isHasNumber(num) { }
    addNumber(obj) {
        obj.boundingBoxp1;
    }
}
